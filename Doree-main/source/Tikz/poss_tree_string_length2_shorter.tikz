%Used in \Cref{sec_listing}

\begin{tikzpicture}[
    grow=right,
    level distance=2.2cm,
    sibling distance=4.8cm,
    every node/.style={anchor=west},
    level 2/.style={sibling distance=2.4cm},
    level 3/.style={sibling distance=1.2cm}
]
\node {$\bullet$}
    child { node {\texttt{c}}
        child { node {\texttt{6 -> a6}} }
        child { node {\texttt{2 -> a2}} }
    }
    child { node {\texttt{b}}
        child { node {\texttt{6 -> b6}} }
        child { node {\texttt{2 -> b2}} }
    }
    child { node {\texttt{a}}
        child { node {\texttt{6 -> a6}} }
        child { node {\texttt{2 -> a2}} }
    };
\end{tikzpicture}